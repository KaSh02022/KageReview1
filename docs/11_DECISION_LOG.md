# 11 — Decision Log

Every major technical/product decision, recorded at the time it is made. Nothing here is retroactively rewritten to look cleaner than the reasoning actually was.

| ID | Date | Decision | Rationale | Alternatives Considered | Impact |
|---|---|---|---|---|---|
| D-001 | 2026-09-23 | Use React 18 + TypeScript with Vite as the build tool | SRS explicitly permits ReactJS (p.16); Master Directive assigns React specifically to Claude's responsibility; TypeScript reduces risk across ~76 requirements and a large static JSON schema; Vite gives a fast, backend-free static build appropriate for a no-server SPA | Angular (also SRS-permitted, but not the Master Directive's assigned framework); plain HTML/CSS/jQuery/Bootstrap (SRS-permitted, but weaker for the scale of stateful cross-cutting features like cart/bookmarks/search) | Sets the entire Phase 1 tooling baseline (`02_PRODUCT_ARCHITECTURE.md` §1) |
| D-002 | 2026-09-23 | Use `HashRouter` from React Router for SPA routing | Satisfies CR-004 (SPA, no backend) without requiring server-side rewrite rules; works even on `file://` or a plain static file server for demo purposes, which matters given DR-006/DR-007 (ZIP submission, optional hosting) | `BrowserRouter` with host-specific redirect rules (adds a hosting-dependent configuration step not guaranteed to be available at submission time) | Final routing shape confirmed in `02_PRODUCT_ARCHITECTURE.md` §9 as an open item pending Phase 1/15 hosting decision |
| D-003 | 2026-09-23 | Use Three.js via React Three Fiber, scoped only to the cinematic entry layer, with mandatory WebGL/reduced-motion fallback | Master Directive assigns Claude the Three.js Engineer role and requires the cinematic layer to "support the product rather than replace the required functionality"; a declarative R3F approach keeps the 3D layer composable with React state (skip control, fallback) rather than a separate imperative Three.js app | A separate static (non-3D) landing page (would under-deliver on the Master Directive's cinematic-experience goal); a full site-wide 3D framework (would risk NFR-005 performance and accessibility, and risks becoming "beautiful landing page that fails the SRS") | Governs Phase 4 scope and Phase 13 performance budget (`06_ASSET_BIBLE.md` §4) |
| D-004 | 2026-09-23 | Build a custom, fully in-repo rule-based chatbot engine instead of integrating Tawk.to or Tidio | The SRS *suggests* Tawk.to/Tidio as optional platforms (p.13, p.16) but does not mandate them, and separately *requires* (as a binding constraint, CR-003) that the chatbot not connect to a live external AI service or backend. The Master Directive additionally forbids introducing external runtime dependencies without Director approval. A self-built JSON-driven rule engine satisfies both documents simultaneously with no conflict and no external dependency | Integrating Tawk.to/Tidio (SRS-mentioned but introduces an external SaaS runtime dependency the Master Directive restricts without approval; also typically requires an account/script tag pointing at a third-party service, in tension with "no backend" spirit) | Documented as the resolution to the IR-004 conflict note in `01_SRS_REQUIREMENTS.md` §D; drives Phase 11 implementation |
| D-005 | 2026-09-24 (Director-approved) | Cart persists to `localStorage` via a Zustand `persist` middleware, scoped to a dedicated storage key (e.g. `fandomverse.cart.v1`) | Project Director resolved the Phase 0 open item explicitly: `localStorage` for temporary client-side cart persistence. This is a *browser-local convenience*, not an account/order cart — it must not imply server persistence, checkout, or order history. Copy/UX near the cart (per `03_UX_ARCHITECTURE.md` §8) continues to state explicitly that the cart is temporary/demo-only and clearing browser storage clears it | In-memory only (rejected — Director specified `localStorage`) | Implemented in Phase 1 as `src/stores/cartStore.ts`; documented in `02_PRODUCT_ARCHITECTURE.md` §4/§9 and `05_DATA_SCHEMA.md` §13 |
| D-006 | 2026-09-24 (Director-approved, refined) | Contact Us map architecture: a responsive, client-only Google Maps `output=embed` iframe (no API key, no backend) as the base map, with (a) a "Get Directions" link that opens Google Maps' own directions UI in a new tab pre-filled with FandomVerse's location as destination, and (b) an *optional*, user-initiated browser Geolocation lookup (`navigator.geolocation.getCurrentPosition`) used only to compute a "directions from your location" link — never to auto-collect or transmit location anywhere, since there is no backend to send it to | Satisfies FR-039 (location + GPS) and the Director's refinement: no backend dependency, no exposed secret key, directions capability, explicit-permission-gated geolocation, and a graceful fallback (if geolocation is denied/unavailable, the map and a manual "Get Directions" link still work fully — geolocation is a progressive enhancement, not a requirement to use the page) | Google Maps JavaScript API with a key (rejected — implies key management/billing, out of scope); auto-requesting geolocation on page load without user action (rejected — poor UX and a needless permission prompt; must be user-initiated, e.g. a button) | Documented in `02_PRODUCT_ARCHITECTURE.md` §4, `03_UX_ARCHITECTURE.md` §11, `08_LICENSES.md` §3 |
| D-007 | 2026-09-23 | Bootstrap the project with `docs/` as the first artifact (Phase 0), with git initialization deferred to Phase 1 (T1.1) | Master Directive's Phase 0 scope is explicitly documentation/audit only, "do NOT build the application yet"; the repository was empty and not yet a git repo at Phase 0 start | Initializing git immediately at Phase 0 (not explicitly required by the directive's Phase 0 instructions, and application scaffolding/git setup is explicitly listed under Phase 1 "Architecture and technical foundation") | Recorded as a Phase 0 assumption in the completion report; git init is the first Phase 1 task (T1.1) |
| D-008 | 2026-09-24 | Performance baseline is defined as a fixed set of *measurement categories* (not numeric targets) to be tracked from Phase 1 onward and given real numeric targets only once Lighthouse/bundle tooling can run against an actual build | The Director explicitly directed against inventing arbitrary final performance claims in Phase 1, while still wanting measurable categories established early so later phases have a consistent yardstick | Committing to specific numeric targets now (rejected — would be fabricated, since no build exists yet to measure against) | Categories recorded in `09_TEST_STRATEGY.md` §9; numeric targets remain an open item for Phase 13 |
| D-009 | 2026-09-24 | Project structure uses `src/{app,components,data,features,hooks,layouts,pages,routes,stores,styles,types,utils}` (Director-specified baseline, adopted as-is) | Director's Phase 1 instructions specify this structure directly; it cleanly separates route-level pages from reusable feature logic, cross-cutting stores, and pure UI primitives, and matches the module boundaries already implied by `02_PRODUCT_ARCHITECTURE.md` §4 | A flatter `src/features/*`-only structure from the original Phase 0 architecture draft (superseded by the Director's explicit structure) | Implemented in Phase 1 scaffold; `02_PRODUCT_ARCHITECTURE.md` updated to match |
| D-010 | 2026-09-24 | Use React 19.2 (latest stable) rather than the Phase 0 draft's "React 18", and pin ESLint to the 9.x line (not the newly-released 10.x) | `npm create vite@latest` on this date scaffolds React 19.2 by default — React 18 was a Phase 0 proposal, not a hard constraint, so latest-stable was taken. ESLint 10 was tried first but `eslint-plugin-jsx-a11y@6.10.2` (needed for the accessibility-linting foundation, §11) only declares peer support up to ESLint 9 as of this date; downgrading to ESLint 9.39.x (still current, released 2026) avoided a `--legacy-peer-deps` workaround that would mask a real incompatibility | `--legacy-peer-deps`/`--force` on ESLint 10 (rejected — papers over an actual unmet peer dependency rather than resolving it); staying on React 18 explicitly (rejected — no functional reason to pin below latest stable, and `@react-three/fiber` 9.x / `@react-three/drei` 10.x both support React 19) | `package.json` dependency versions; noted here so the version choice isn't mistaken for silent scope drift from `02_PRODUCT_ARCHITECTURE.md` |
| D-011 | 2026-09-24 | Fixed two real issues surfaced by an actual Lighthouse run against the Phase 1 build: (1) the cinematic entry's Suspense fallback didn't reserve the same footprint as the loaded content, causing a layout shift (CLS 0.313 → 0 after fix); (2) no `public/robots.txt` existed (SEO score 91 → 100 after adding a permissive one) | Director explicitly asked Phase 1 to establish measurable performance categories (D-008); running the actual tool surfaced two cheap, clearly-in-scope defects rather than leaving them for Phase 13 — fixing a layout-shift bug in code just written is ordinary correctness work, not premature optimization | Leaving both for Phase 13 (rejected — CLS is a correctness bug in code delivered this phase, not a tuning question; the fixes cost two small edits, not an optimization pass) | `src/features/universe/CinematicEntry.tsx`, `public/robots.txt`; real before/after numbers recorded in `09_TEST_STRATEGY.md` §9 |
| D-012 | 2026-09-24 | Fixed a real focus-stealing bug in `ChatbotLauncher`: its open/close focus-management effect fired on initial mount (since it was keyed on `isOpen`, which starts `false`), calling `.focus()` on the chatbot trigger button before the user did anything — this silently broke the skip-link/keyboard-navigation foundation (Tab landed on the chatbot button instead of the skip link) | Found via a genuine E2E failure (`e2e/accessibility.spec.ts`), not by inspection — the Playwright run against the real built app caught a bug static review missed | Leaving the effect as-is and instead changing the skip-link/tab-order test to tolerate it (rejected — that would hide a real accessibility defect instead of fixing it) | `src/components/ChatbotLauncher/ChatbotLauncher.tsx` — a `hasOpenedRef` guard now prevents the close-focus-restore behavior from firing on mount |
| D-013 | 2026-09-24 | Fixed a real breadcrumb bug: the root layout route had no `handle.breadcrumb`, so every route (even nested category pages) produced at most 1 crumb, and `Breadcrumb` hides itself at `crumbs.length <= 1` — meaning the breadcrumb never rendered anywhere, including on category pages where FR-044 requires it | Found via E2E (`e2e/navigation.spec.ts`'s breadcrumb test), confirming the router's `useMatches()`-based derivation (docs/02_PRODUCT_ARCHITECTURE.md §3) needs a `handle` on every ancestor route, including the root, not just leaf routes | Lowering the visibility threshold to `crumbs.length === 0` instead (rejected — would show a bare, non-clickable "Home" crumb on the Home page itself, which is not useful) | `src/routes/routes.tsx` — root route now carries `handle: { breadcrumb: 'Home' }` |
| D-014 | 2026-09-24 | Playwright config: capped `workers` to 4 (was unset/auto, which used 10 on this 20-core machine) and set `retries: 1` locally | A full 5-project (chromium/firefox/webkit/mobile-chrome + retries) run at full auto-parallelism produced one single-test timeout (`navigating into a category hub` on `mobile-chrome`) that did not reproduce when the same test was run in isolation — consistent with resource contention across many concurrent browser instances in a constrained sandbox, not an app defect | Increasing the individual test's timeout instead (rejected — treats a symptom of parallel contention as if it were slow app code, and wouldn't address the root cause) | `playwright.config.ts` |
| D-015 | 2026-09-24 | Accept WebKit's default "links aren't Tab-focusable" behavior as a documented, skipped platform limitation for the skip-link E2E assertion, rather than changing the skip link's markup to work around it | This is WebKit's actual default (Safari requires the user's own "Full Keyboard Access" OS setting for `<a>` elements to receive Tab focus) — identical behavior would affect any site's anchor-based skip link in default Safari, so it isn't specific to FandomVerse's implementation, and WCAG does not require overriding a platform's own keyboard-navigation model | Changing the skip link to a `<button>` with `history.pushState`-based navigation (rejected — loses the plain, robust `<a href="#main-content">` semantics for every other browser/AT combination to work around one engine's opt-in default) | `e2e/accessibility.spec.ts` — test explicitly skipped for `browserName === 'webkit'` with the reason recorded inline |
| D-016 | 2026-09-25 | FandomVerse is dark-first **and dark-only** — no light theme, no theme toggle | Director's Phase 2 instructions (§20) asked to inspect the UX/design docs and only build a toggle if justified; `00_PROJECT_CONSTITUTION.md`/`04_DESIGN_SYSTEM.md` have described a dark cinematic "fandom universe" concept since Phase 0, and no SRS requirement or UX doc calls for a light mode. Building one now would be an unrequested duplicate visual system, explicitly against the Director's guidance | A toggle "for feature completeness" (rejected — no requirement asks for it, and it would double the token/contrast-verification surface for no product reason) | `src/styles/tokens.css` sets `color-scheme: dark` at the token layer with no light-mode branch; documented in `04_DESIGN_SYSTEM.md` §1 |
| D-017 | 2026-09-25 | Built a single shared `Dialog` primitive (`src/components/ui/Dialog`) and a `Drawer` primitive (`src/components/ui/Drawer`), both backed by one `useFocusTrap` hook, and rebuilt `ChatbotLauncher`/`DummyAuthModal` on top of `Dialog` instead of their Phase 1 hand-rolled implementations | The Phase 1 audit (Director §4/§13) found neither hand-rolled dialog actually trapped Tab focus (only Escape + a subset of focus restoration existed) — a real, unnoticed a11y gap. A shared primitive means this can only be implemented correctly once, and any third future dialog (Phase 6+ filters, Phase 9 cart drawer, etc.) inherits the fix automatically | Leaving each dialog to hand-roll its own focus management (rejected — this is exactly how the Phase 1 gap happened, and how the exact Phase 1 focus-stealing bug (D-012) could silently reappear in new code) | `src/hooks/useFocusTrap.ts`, `src/components/ui/Dialog/`, `src/components/ui/Drawer/`; regression tests in `src/components/ui/Dialog/Dialog.test.tsx` and `src/components/ChatbotLauncher/ChatbotLauncher.test.tsx` |
| D-018 | 2026-09-25 | Reconciled the Header's mobile-nav collapse breakpoint from the Phase 1 arbitrary `767px` to the canonical tablet/desktop boundary (`max-width: 1023px`, matching `--breakpoint-tablet-max` / `src/styles/breakpoints.ts`'s `tabletMax`) | Director's Phase 2 instructions (§5) explicitly flagged this Phase 1 inconsistency and asked for one canonical breakpoint system used consistently. A 10-link category bar plus search/utility icons doesn't comfortably fit until the desktop tier (≥1024px), so both mobile *and* tablet now get the drawer nav, rather than tablet getting a cramped inline bar | Keeping 767px as its own ad hoc "large phone" breakpoint (rejected — reintroduces exactly the arbitrary-value problem the Director asked to fix); using a 5th custom breakpoint just for the header (rejected — the existing 4-tier scale already has a boundary that fits this need) | `src/components/Header/Header.module.css`, `src/components/Header/Header.tsx` |
| D-019 | 2026-09-25 | Canonical route inventory: 23 distinct route patterns (24 table rows including the non-URL root layout route), documented once in `02_PRODUCT_ARCHITECTURE.md` §3 as the single source of truth for route counts in future reports | The Phase 1 completion report's "21 routes" (from the console/network audit's scope) and the fuller implemented route list (which also includes the generic `/category/:slug` alias and the `*` Not Found route) were both correct for what they described, but reading them side by side without context looked like an inconsistency — Director's Phase 2 §23 asked this to be resolved explicitly, not just left implicitly correct | Silently changing one of the counts to match the other (rejected — no route was actually missing or extra; the fix is a documentation clarification, not a code change) | `02_PRODUCT_ARCHITECTURE.md` §3 |
| D-020 | 2026-09-25 | Confirmed React 19.2 as the formal architecture baseline (not just a Phase 1 tooling accident) | Director's Phase 2 §6 asked for explicit compatibility verification before treating React 19 as settled. Verified: `@react-three/fiber` 9.x and `@react-three/drei` 10.x both declare React 19 support and the cinematic entry (Phase 1) already exercises this path in E2E; `react-router-dom` 7.18 lists React 19 as a supported peer and its data-router APIs (`createHashRouter`, `useMatches`) are exercised throughout the route table and the Breadcrumb regression test; Zustand 5's `create`/`persist` API is version-agnostic (no React-version-specific behavior) and is exercised by 3 passing store test suites; `@testing-library/react` 16.x is the version that added React 19 support; Playwright drives the built app as a real browser and is React-version-agnostic by construction. No React-version-related runtime warnings appeared in any of the 45 unit tests, 40+ E2E tests, or the manual console/network audit across 21 routes × 4 browsers (Phase 1) — repeated clean in Phase 2 (§16 below) | Downgrading to React 18 "to match the original Phase 0 draft" (rejected — Phase 0's React 18 was a proposal made before any tooling existed, not a hard requirement, and downgrading now would require re-verifying the same dependency graph in the *other* direction for no functional benefit) | `docs/02_PRODUCT_ARCHITECTURE.md` §1 stack table now lists React 19.2 as confirmed, not provisional |
| D-021 | 2026-09-25 | Fixed a real bug in `useFocusTrap`'s hidden-element filter: it used `el.offsetParent !== null` to exclude invisible elements from the Tab cycle, which is always `null` in jsdom (no layout engine) — this silently emptied the focusable-elements list in every unit test, not just a test-authoring mistake. Replaced with a `getComputedStyle`-based `display`/`visibility` check, which works correctly in both real browsers and jsdom | Found while writing `Dialog.test.tsx`'s Tab-wrapping regression test (§13/§22 of the Phase 2 brief) — the first version of that test failed because focus never moved at all, which traced back to this filter, not the test itself | Skipping the visibility filter entirely (rejected — real browsers do need it, to exclude conditionally-hidden focusable elements from the trap; the jsdom-safe computed-style version keeps the real protection without the false-empty bug) | `src/hooks/useFocusTrap.ts` |
| D-022 | 2026-09-25 | Fixed a real prop-forwarding bug in `Card`: when rendered with a `to` prop (as a link), it dropped every prop in `...rest` (e.g. `aria-label`, `data-testid`, event handlers) — only the non-link `<div>` branch forwarded them. Also widened `CardProps` from `HTMLAttributes<HTMLDivElement>` to `HTMLAttributes<HTMLElement>` so the same prop set type-checks against both the `<div>` and the router `<Link>` (`<a>`) branches | Found while writing `Card.test.tsx`'s prop-forwarding test | Leaving `to`-Cards unable to accept extra props (rejected — every real future usage, e.g. an accessible label beyond the card's heading text, would silently be dropped) | `src/components/ui/Card/Card.tsx` |
| D-023 | 2026-09-25 | Fixed a real, significant horizontal-overflow bug: the Header's utility bar (visitor counter + clock text + bookmark/cart icons + auth button, all `display:flex` with no wrap) had no mobile compaction, producing ~703px of content in a 375px viewport on every route — nearly 2× overflow. Fixed by (1) wrapping the visitor counter + clock in a `.metaGroup` that's hidden at the canonical mobile tier (≤599px, docs/04_DESIGN_SYSTEM.md §2a) since they're secondary/decorative and FR-041/FR-042 don't require them on every viewport, and (2) adding `flex-wrap: wrap` to `.topRow` as a general safety net so any future overflow degrades to wrapping, never hard clipping | Found by `e2e/responsive.spec.ts`, a new Phase 2 test added specifically because the Director's §17/§25 instructions asked for real horizontal-overflow checks across the canonical viewport tiers — this is exactly the kind of defect that inspection alone would not have caught, since it only manifests below ~600px | Hiding the whole utility bar including bookmarks/cart/login at mobile (rejected — those are functional navigation, not decorative, and must stay reachable per SRS p.6); increasing the mobile breakpoint further (rejected — doesn't address the actual cause, just moves where it breaks) | `src/components/Header/Header.tsx`, `src/components/Header/Header.module.css`; verified with 80 passing checks across 5 viewports × chromium/mobile-chrome in `e2e/responsive.spec.ts` |
| D-024 | 2026-09-25 | Fixed a real visual bug found via actual Playwright screenshots (Director §25): the cinematic entry's "FandomVerse" `<h1>` used the fixed `--font-size-4xl` (56px) token regardless of viewport, and at 375px mobile width the text was wide enough to clip against the hero's own `overflow:hidden` edge (confirmed visually — the drawer-open screenshot showed a stray "se" fragment bleeding through from the clipped heading behind it). Fixed by switching to `--font-display`, the fluid `clamp(2.5rem, 2rem + 2vw, 4rem)` token built for exactly this case, plus horizontal padding on the hero container | This was invisible to every automated check run so far (typecheck/lint/unit/E2E overflow assertions all passed) because `document.documentElement.scrollWidth` doesn't detect content clipped by an ancestor's `overflow:hidden` — only an actual rendered screenshot surfaces it, which is exactly why the Director's Phase 2 brief asked for real screenshot-based visual QA in addition to automated checks, not as a redundant step | Reducing the hero height/font further as a fixed value (rejected — reintroduces the same class of bug at some other viewport width instead of using the fluid token that already exists for this) | `src/features/universe/CinematicEntry.module.css` |
| D-025 | 2026-09-26 | Formally closed the Phase 1/2 "dummy login persistence" open item: **in-memory only**, no `localStorage`/`sessionStorage` | Director's Phase 3 instruction asked for "the simplest architecture consistent with the SRS." In-memory is simpler than adding a persisted store key, and — since the state carries no real identity — persisting it risks looking like a real remembered session to an observer, undercutting the UI-only disclaimer already shown in the dialog. No SRS requirement asks for the logged-in state to survive a reload | `sessionStorage` persistence "for a more convincing demo" (rejected — the Director's instruction explicitly prioritizes simplicity and SRS-consistency over demo polish here; also risks the exact "looks like real auth" ambiguity this decision avoids) | No code change (this was already Phase 1's behavior); `02_PRODUCT_ARCHITECTURE.md` §11 documents it as resolved, closing the open item from `10_REMAINING_OPEN_ITEMS`/§10 |
| D-026 | 2026-09-26 | Added a per-route document title (`src/components/DocumentTitle/DocumentTitle.tsx`), reading `handle.title` from the route table via `useMatches()` — the same pattern already used for `Breadcrumb` | Real Phase 3 audit finding: every route showed the identical static `index.html` title, with no way to distinguish routes in browser tabs/history/bookmarks — an SRS-adjacent usability gap (§9 of the Director's brief) that had gone unnoticed through Phases 1–2 since no test checked `document.title` | A per-page `useEffect(() => { document.title = ... })` hand-written in each of 17 page components (rejected — exactly the "hand-maintained per page, easy to forget on the 18th page" pattern `Breadcrumb`'s route-data-driven design was built to avoid) | `src/routes/routes.tsx` (`handle.title` on every route), `src/components/DocumentTitle/DocumentTitle.tsx`, `src/pages/RouteErrorFallback.tsx` (sets its own title since it replaces `RootLayout` entirely); regression-tested in `src/app/RouteTransitions.test.tsx` |
| D-027 | 2026-09-26 | Added scroll-to-top + focus-to-main-content on route change (`src/hooks/useRouteTransitionEffects.ts`), skipped on initial load and on same-page query-string-only changes | Real Phase 3 audit finding: neither scroll position nor focus reset on navigation — a screen-reader user got no indication a route change had happened, and a user navigating from a scrolled-down page landed on the new page already scrolled down. Both are common, well-documented SPA accessibility/UX gaps | Resetting on every `location` change including search-param changes (rejected — would reset scroll/focus on every search-query keystroke-triggered update, which is disruptive, not helpful); scrolling only, no focus move (rejected — leaves the screen-reader-announcement half of the gap unfixed) | `src/hooks/useRouteTransitionEffects.ts`, `src/layouts/RootLayout.tsx`; regression-tested in `src/app/RouteTransitions.test.tsx` and exercised end-to-end in `e2e/keyboard-walkthrough.spec.ts` |
| D-028 | 2026-09-26 | Audited route-level code splitting (Director §1) and explicitly deferred it to Phase 13, rather than implementing it now | All 17 page components currently bundle into one 364KB (113KB gzip) main chunk. Implementing `React.lazy`/`route.lazy` cleanly would require reworking `CategoryHubPage`'s prop-based category selection (currently passed from route config, which doesn't compose with React Router's `route.lazy` API), a real refactor — not a "harden what exists" change, and both Phase 1's and this phase's instructions explicitly reserve performance optimization for Phase 13 | Implementing it now anyway since it was flagged in the audit checklist (rejected — an audit item can conclude "not yet warranted, deferred with a documented reason," which is what happened here; the current bundle size isn't causing a measured problem) | Documented in `02_PRODUCT_ARCHITECTURE.md` §13; no code change |
| D-029 | 2026-09-26 | Reconfirmed dark-first/dark-only as the project-wide theme strategy — no light theme, no theme toggle (formally re-affirms D-016 under this phase's explicit re-verification request) | Director's Phase 3 §7 asked for one explicit, justified project decision. Re-checked against: visual identity (the "fandom universe" concept is built around a dark canvas with glowing category accents — a light theme would need an entirely separate, independently-verified token set, not a mechanical inversion); accessibility (dark-only means only one contrast matrix needs verifying, not two); category accents (all 7 were chosen and tuned against the dark surface tokens — untested against a light background); future WebGL integration (a light background would wash out the cinematic entry's glow/depth effects Phase 4 is scoped to build); implementation complexity (a second theme roughly doubles the token-verification and component-QA surface for a feature no SRS requirement or UX document asks for) | Adding a light theme (rejected — no requirement drives it, and Director's own §7 instructs not to add a toggle "merely for decoration"); a toggle that only swaps accent saturation, not full light/dark (rejected as a half-measure — would need its own justification and testing burden with even less product rationale than full light mode) | No code change (already the case since Phase 1/2, `color-scheme: dark` in `tokens.css`); `11_DECISION_LOG.md` (this entry) and `04_DESIGN_SYSTEM.md` §1 are the record of the reconfirmation |
| D-030 | 2026-09-26 | Fixed the new keyboard-walkthrough E2E test itself (not an app defect): its "HOME -> CATEGORY..." leg assumed a desktop viewport (to reach the inline category nav bar) but didn't set one explicitly, so it failed on `mobile-chrome` (nav hidden below the canonical breakpoint) and on `webkit` (D-015's already-documented default: `<a>` links aren't in Tab order without Full Keyboard Access) | Found by running the new test across all 4 browser projects, not just chromium | Redesigning the app's nav to be Tab-reachable identically on every viewport/engine (rejected — mobile nav is deliberately behind a Drawer per the responsive design, and the WebKit behavior is a platform default outside the app's control, already accepted for the skip-link test) | `e2e/keyboard-walkthrough.spec.ts` — explicit `setViewportSize({width:1280,height:800})` and a `browserName === 'webkit'` skip, consistent with D-015 |
| D-031 | 2026-09-27 | Fandom Core architecture: original central "Core" object (nested icosahedron geometries + point light), seven category nodes, a starfield, and camera parallax — all procedural (Three.js primitives via R3F), no external models/textures. The seven nodes are represented in TWO layers on purpose: a small set of inner, category-tinted "energy fragments" in the WebGL scene (atmospheric depth only) and one set of real, accessible HTML links (`FandomCoreOverlay`) positioned in a ring via CSS `cos()`/`sin()` trig functions, laid entirely independently of the 3D camera/projection | Director's Phase 4 brief requires the WebGL canvas to never be the only interaction surface, and requires the exact same navigation to survive if WebGL disappears entirely. Projecting real 3D node positions into 2D screen space every frame (to keep HTML controls precisely synced to 3D nodes) is fragile (breaks under camera parallax/resize) and unnecessary — decoupling the accessible layer from the 3D layer entirely satisfies both the "no WebGL-only interaction" and "identical fallback" requirements structurally, not by convention | Making the 3D meshes themselves the click targets via raycasting (rejected — canvas-only interaction, fails Director's explicit "do not make a WebGL canvas itself the only interaction surface"); projecting 3D node positions to sync HTML overlay position every frame via `Vector3.project()` (rejected — adds per-frame computation and resize-handling complexity for a purely cosmetic sync, and still needs a non-3D fallback overlay anyway, so it doesn't reduce the amount of code, only adds fragility) | `src/features/universe/{FandomCoreScene,FandomCoreOverlay,FandomCoreFallback,CinematicEntry}.tsx`, `fandomCoreNodes.ts` |
| D-032 | 2026-09-27 | **Fixed a serious, pre-existing accessibility/functional bug, present since Phase 1**: both `SkipLink` (`href="#main-content"`) and the new Fandom Core's skip-intro control used a plain anchor with a bare `#id` href. In this `HashRouter` app, letting the browser follow that native anchor jump rewrites `window.location.hash` to `#id` — which the router then reads as an attempted route path, silently replacing the entire current page with the Not Found route. Concretely: a keyboard user on `/#/anime` pressing Tab then Enter (the single most basic accessibility affordance in the app) was thrown onto a "Page not found" screen. Fixed by calling `event.preventDefault()` and moving focus (and, for the Fandom Core, scrolling) purely via JS, never letting the browser touch `location.hash` for same-page navigation | Found via Phase 4 E2E testing of the *new* skip-intro control, which reused the same href pattern as the year-old `SkipLink` — testing the new code surfaced a latent bug in old code that had shipped through 3 prior Director-approved phase gates. Every previous accessibility test checked that focus *moved* after activating the skip link, but never checked that the *route/page content* was still correct afterward, so the regression was invisible to all prior automated and manual verification | Leaving `SkipLink` as-is since it predates this phase (rejected — an active, severe, user-facing bug doesn't become acceptable because it's old; "harden the shell" per Director's own recurring instruction includes fixing bugs found in the existing shell, not only in new code) | `src/components/SkipLink/SkipLink.tsx`, `src/features/universe/CinematicEntry.tsx`; regression tests added in `e2e/accessibility.spec.ts` (non-root-route case) and `e2e/fandom-core.spec.ts` |
| D-033 | 2026-09-27 | No external or AI-generated visual assets were introduced this phase — every element of the Fandom Core (core geometry, category fragments, orbit ring, starfield, gradient glow) is procedural code (Three.js primitives, CSS gradients/`color-mix()`), per the Director's Phase 4 §20 instruction to prefer procedural geometry and stop before integrating any external asset | The vertical slice's visual goals (cinematic, layered, original) were fully achievable without textures or models, and the Director's brief explicitly frames external assets as something to justify before adding, not a default | Generating hero/core imagery via Gemini now (rejected — not needed for this vertical slice, and premature relative to the Director's own phasing, which reserves asset generation for a later, explicitly-scoped step) | `docs/06_ASSET_BIBLE.md`, `docs/08_LICENSES.md` — both explicitly confirm zero new assets this phase, not silently left unchanged |
| D-034 | 2026-09-27 | Extracted `CATEGORY_ROUTES` out of `routes.tsx` into a new, dependency-free `src/routes/categoryRoutes.ts`, with `Header`, `HomePage`, and the new `fandomCoreNodes.ts` all importing from there directly instead of through `routes.tsx` | Real bug found while wiring the Fandom Core: `fandomCoreNodes.ts` (needed by `HomePage` via `CinematicEntry`) importing `CATEGORY_ROUTES` from `routes.tsx` created a circular import (`routes.tsx` → `HomePage` → `CinematicEntry` → ... → `fandomCoreNodes.ts` → `routes.tsx`), which left `CATEGORY_ROUTES` `undefined` at evaluation time and crashed/hung the test suite | Leaving `fandomCoreNodes.ts` importing from `routes.tsx` and reordering imports to work around evaluation order (rejected — fragile, depends on bundler-specific module evaluation order rather than eliminating the cycle; a genuinely acyclic import graph is the correct fix) | `src/routes/categoryRoutes.ts` (new), `src/routes/routes.tsx`, `src/components/Header/Header.tsx`, `src/pages/HomePage.tsx`, `src/features/universe/fandomCoreNodes.ts` |
| D-035 | 2026-09-27 | Fixed a real visual defect found via required Phase 4 screenshot QA (not caught by any automated test): the 3D category fragments and the HTML overlay's category dots were both drawn at approximately the same orbit radius, producing two visibly separate, unaligned rings of seven marks instead of one coherent set of nodes. Fixed by deliberately separating the radii — `RING_RADIUS` (outer, matches the HTML overlay, marked by the visible torus ring) and `FRAGMENT_RADIUS` (inner, smaller, for the 3D meshes) — reframing the inner meshes as atmospheric depth behind the real interactive ring, consistent with the Director's suggested "layered depth" visual language | This is exactly the class of defect the Director's Phase 4 §18 anticipated ("do not declare visual quality based only on automated tests") — every unit/E2E/axe/typecheck/lint check passed while this was live, because none of them render and inspect pixels | Attempting to precisely 3D-project the fragments onto the HTML ring's exact CSS positions (rejected — see D-031's rationale against per-frame projection sync) | `src/features/universe/FandomCoreScene.tsx` |

| D-036 | 2026-09-23 | Every Phase 5 visual asset is a **procedurally generated SVG**, authored by `scripts/generate-content.mjs` and committed to `public/assets/generated/` (161 files: 7 category heroes, 35 character portraits, 28 gallery pieces, 21 event banners, 21 article thumbnails, 21 release covers, 14 trailer thumbnails, 14 merchandise tiles). Gradients/shapes/initials are drawn from code, seeded deterministically by each entity's own id | The Director's Phase 5 §18 requires provenance for every asset and forbids inventing license information. A generated-from-code asset has unambiguous provenance (this repo's own script, re-runnable to reproduce byte-identical output), zero third-party rights, and zero risk of accidentally ingesting franchise artwork. It also keeps FandomVerse's "no external assets" posture from D-033 intact | Sourcing stock/royalty-free imagery (rejected — every file would need an individually verified license row for a student project that gains nothing from it); AI image generation via Gemini (deferred — still permitted by the SRS and Asset Bible §9, but it would introduce a provenance/ownership question the Director has not yet ruled on, and procedural art is sufficient for Phase 5's purpose) | `scripts/generate-content.mjs`, `06_ASSET_BIBLE.md`, `08_LICENSES.md` §1/§4 |
| D-037 | 2026-09-23 | All seven categories are anchored to **one original flagship franchise each** (Anime → *Starlit Ronin*, Gaming → *Ashfall Protocol*, Movies → *Midnight Meridian*, TV → *The Glass Archive*, K-Pop → *LUNARIS*, Comics → *Ironclad Vanguard*, Manga → *Paper Moon Requiem*), with all 35 characters, 21 articles, 21 events, 21 releases and 14 merchandise items written as that franchise's fandom | Gives each hub genuine internal coherence (characters, articles, events and merch all reference each other) instead of 7 unrelated content piles, while guaranteeing zero copyright exposure: no real IP, likeness, logo, or title appears anywhere in the dataset. It also makes `relatedIds` meaningful rather than arbitrary | Using real franchises (rejected outright — copyright, and the SRS forbids it); generating many unrelated fictional properties per category (rejected — more content for less coherence, and the Director explicitly warned against "huge amounts of arbitrary text") | `scripts/generate-content.mjs`, `05_DATA_SCHEMA.md` §14 |
| D-038 | 2026-09-23 | Events, releases, and trailers each carry an explicit `fictional: true` field, surfaced in the UI as a "Simulated fan event" / "Fictional trailer" badge, and trailer `embedUrl` is an empty string rather than any external video URL | The Director's §7/§10/§12 require that simulated content be labelled so users are never misled, and §10 forbids depending on an external streaming API. A structured boolean (rather than only prose) makes the labelling **testable** — `contentValidation.test.ts` asserts every event/release/trailer is flagged and that no trailer ships a live external embed | Labelling only in description prose (rejected — unverifiable and easy to drop silently); embedding real royalty-free sample video (rejected — an external dependency for content that depicts a fictional franchise anyway) | `src/types/content.ts`, `src/pages/CategoryHubPage.tsx`, `src/data/contentValidation.test.ts` |
| D-039 | 2026-09-23 | **Fixed a real WCAG AA contrast failure in the Phase 2 `Badge` component**: `tone="primary"` rendered `--color-primary` (#5b7bff) on its own 14%-opacity tint (effective #1e2847) at 3.94:1, below the 4.5:1 AA threshold for small text. Added a `--color-primary-on-tint` token (#8da4ff) and used it for that tone | Found by the new axe scan over a fully populated category hub. The defect has existed since Phase 2 but never surfaced, because no page rendered a primary-tone badge until Phase 5 shipped real content — the same class of latent defect as D-032, exposed only once real usage existed. Fixed at token level so every current and future `tone="primary"` usage is correct | Overriding the colour only where the badge is used (rejected — leaves the component itself broken for the next caller) | `src/styles/tokens.css`, `src/components/ui/Badge/Badge.module.css`, `e2e/category-hubs.spec.ts` |
| D-040 | 2026-09-23 | Fixed three visual defects found only through required real-screenshot QA: (a) card art was drawn on wide artboards (900×420, 640×360, 640×800) but `CardMedia` crops to 4:3, slicing titles off at both edges — all card art is now authored 4:3; (b) each card drew its own title into the image *and* rendered it again as a heading directly beneath, so titles now live only in the DOM and the art carries a short franchise/type label instead; (c) the merchandise "shirt" glyph path was malformed and rendered as an arrow-like blob, and the four gallery tiles per category shared identical initials — now varied per piece (`PM·1`…`PM·4`) | Exactly the defect class the Director's §24 targets: typecheck, lint, 47 content-validation assertions, axe, and the full E2E suite all passed while every one of these was live on screen. Only looking at rendered pixels caught them | — | `scripts/generate-content.mjs` |
| D-041 | 2026-09-23 | Category hub grids for characters/gallery/releases/merchandise use `minItemWidth: 150` so they resolve to two columns at 375px instead of one | Full-page mobile screenshots showed a populated hub running 9,572px tall with every card full-width — technically passing every overflow test, but genuinely unpleasant to scroll. Two columns cut it to 7,475px (-22%) with no overflow at any of the five required viewports | Truncating sections on mobile with "show more" (rejected — hides SRS-required content behind interaction and adds state this phase doesn't need) | `src/pages/CategoryHubPage.tsx` |
| D-042 | 2026-09-24 | Phase 5B produces a **plan only**: prompts, manifest, provenance workflow and review gate are authored, but no image is generated and no runtime data references a generated filename. The 161 procedural SVGs stay as the shipping assets | The Director's Phase 5B directive is explicit ("Do NOT generate images", "Do NOT replace the current 161 procedural SVG assets yet"). Separating specification from generation also means the expensive step happens against a fixed, reviewed spec rather than being improvised | Generating a sample set to validate the style (rejected — explicitly out of scope this phase) | `docs/AI_IMAGE_ASSET_BIBLE.md`, `docs/GEMINI_IMAGE_PROMPTS.md`, `docs/ASSET_MANIFEST.md`, `src/data/assetManifest.test.ts` |
| D-043 | 2026-09-24 | Of 161 current assets, **70 are approved for Gemini** (7 category heroes, 35 character portraits, 28 gallery pieces), **56 are deferred** (articles, trailers, releases), and **35 are deliberately kept procedural** (14 merchandise, 21 event visuals) | The directive warned against generating one image per existing SVG. The approved three are where art is load-bearing: the hero is the largest and weakest visual on every hub, character profiles are an SRS-mandated feature currently served by initials-avatars, and a gallery's entire purpose is images. Merchandise art depicts products that do not exist — a photoreal mock-up edges toward misleading on a page that already disclaims real purchase — and event cards carry date/location/type as live text, so their art is decorative | Generating all 161 (rejected — cost without proportional gain, and explicitly warned against); generating only the 7 heroes (rejected — leaves the placeholder-looking character avatars, the most visible weakness, untouched) | `docs/AI_IMAGE_ASSET_BIBLE.md` §6 |
| D-044 | 2026-09-24 | Three canonical image classes only — CLASS-HERO (16:9, 2560×1440), CLASS-CARD (4:3, 1600×1200), CLASS-PORTRAIT (1:1, 1024×1024) — with dimensions derived from **measured** render sizes rather than chosen by convention. A 21:9 cinematic-backdrop class was evaluated and rejected | Measuring first surfaced a constraint that would otherwise have ruined the heroes: the hub hero band renders at ~1392×280 (≈5:1) on desktop but ~327×272 (≈1.2:1) on mobile, so hero art must survive a swing from 5:1 to 1.2:1 and needs an unusually tight central safe area. A backdrop class was rejected because the only full-bleed surface is the Fandom Core, which must stay procedural — adding one would mean inventing a UI slot to justify an asset | Adopting conventional 16:9/3:4/1:1 sizes without measuring (rejected — would have produced heroes cropped to unusable slivers on desktop) | `docs/AI_IMAGE_ASSET_BIBLE.md` §4, `scripts/generate-asset-plan.mjs` |
| D-045 | 2026-09-24 | Accepted Gemini assets are registered in `08_LICENSES.md` as **`AI-generated — usage governed by the generating platform's terms as of the generation date; ownership status not independently adjudicated`**, never as public-domain, copyright-free, royalty-free or owned. Accepted assets get their own rows, separate from the procedural-SVG batch rows. The replaced SVG is always retained | Phases 1–5 could honestly say "original work, no third-party license involved" because every asset was output of a script in this repository. Generated imagery is the first asset class whose rights position is not self-evident, and silently inheriting the old "Verified / no license needed" wording would be a false claim. Keeping the SVG means any asset can be rejected or withdrawn later with a one-line revert | Asserting a specific licence (rejected — no licence has been read and verified, and inventing one is exactly what `00_PROJECT_CONSTITUTION.md` §10 forbids) | `docs/ASSET_PROVENANCE.md` §1, `docs/08_LICENSES.md` |
| D-046 | 2026-09-24 | Prompts are **generated from the live content data** by `scripts/generate-asset-plan.mjs`, not hand-written, and a validator (`scripts/validate-asset-manifest.mjs`, run in CI via `src/data/assetManifest.test.ts`) fails if the plan drifts from the dataset | 70 hand-written prompts would drift the moment a character or franchise was renamed, leaving a prompt describing someone who no longer exists. Deriving them means a rename is a re-run. The validator was confirmed to actually fail by adding a character without regenerating the plan — it reported both the type-count mismatch and the total mismatch | Hand-authoring the prompt document (rejected — 70 near-identical blocks, drift-prone, and error-prone to maintain) | `scripts/generate-asset-plan.mjs`, `scripts/validate-asset-manifest.mjs`, `src/data/assetManifest.test.ts` |
| D-047 | 2026-09-24 | **Developer-facing scaffolding may never appear in the user-facing UI.** The `requirementIds` and `phase` props were removed from `PagePlaceholder` outright (not merely unset), SRS requirement ids were stripped from eleven user-facing strings, and the Phase 1 placeholder address "Seed City, Placeholder Region" was replaced with an honest statement that the project has no premises. Enforced by `e2e/content-honesty.spec.ts` | A Phase 5C screenshot review found the Cart page rendering "Requirements: FR-029, FR-030, FR-031" to visitors. `PagePlaceholder` was built in Phase 1 as a development placeholder and kept its dev affordances after it had quietly become the standard page header. No existing test could catch this: every suite asserted on what *should* be present, and nothing asserted on what must never appear. The replacement copy is also more useful to a visitor — "no checkout, payment, or real purchase" says more than "(FR-031)" | Leaving the props in place but unset (rejected — a future caller reintroduces the leak); suppressing the metadata in production builds only (rejected — adds build-mode branching to solve a content problem) | `src/components/PagePlaceholder/PagePlaceholder.tsx`, 5 pages, 2 components, `e2e/content-honesty.spec.ts`, `docs/PHASE_5C_CONTENT_UX_AUDIT.md` |
| D-048 | 2026-09-24 | Gemini Batch 01 is a **14-asset proof-of-style** — the 7 category heroes plus one canonical lead character per category — drawn as a strict subset of the 70 approved Tier B assets, with prompts extracted verbatim from `GEMINI_IMAGE_PROMPTS.md` rather than re-composed | Validates the shared visual language across all seven worlds on the two hardest image classes before committing to 70 generations. CLASS-HERO has the most punishing crop in the system (~5:1 desktop to ~1.2:1 mobile, D-044) and CLASS-PORTRAIT is the only asset used at two aspect ratios, so if the style survives both, the remainder is low-risk. Leads are selected by an explicit tag rule (`protagonist`, else `leader`) so the choice is reproducible and auditable rather than a preference. Extracting prompts verbatim means the batch physically cannot drift from the approved set | Generating all 70 at once (rejected — a style failure would be discovered 70 images late); a 7-asset heroes-only batch (rejected — would not exercise the portrait crop, the other hard class) | `scripts/generate-batch-01.mjs`, `docs/GEMINI_BATCH_01_*`, `scripts/validate-asset-manifest.mjs` |
| D-049 | 2026-09-24 | Phase 6 search state lives **entirely in URL query parameters** read through `useSearchParams`, with no Zustand store and no component state as source of truth | Shareable, deep-linkable, survives reload and back/forward, needs no new state library, and keeps one source of truth. It also extends a pattern already proven under `HashRouter` — `SearchPage` already reads `?q`, and `deep-links.spec.ts` already asserts `#/search?q=anime` survives a reload. The spec records three guard rules, the most important being that params are read only via `useSearchParams` and never via `window.location.search` (which under HashRouter refers to the pre-hash query), and that a raw `#fragment` is never written — the D-032 defect class | A Zustand search store (rejected — a second source of truth for state the URL already models); component state (rejected — not shareable, lost on reload) | `docs/PHASE_6_SEARCH_FILTER_SORT_SPEC.md` §6 |
| D-050 | 2026-09-24 | FR-008's "popularity" is mapped to the existing `featured` flag, and undated documents sort **last** under date sorts rather than being hidden or given invented dates | There is no popularity signal in the dataset — no view counts, no ratings — and fabricating one would be inventing data. `featured` is the only real editorial-prominence field. Likewise 84 of 161 documents (characters, merchandise, gallery images, categories) legitimately have no date; assigning them one to make a sort look tidy would be fabrication. Both mappings are recorded so the compromise is visible rather than silently assumed | Inventing view counts or synthetic dates (rejected — fabricated data); omitting the sort options (rejected — FR-008 requires them) | `docs/PHASE_6_SEARCH_FILTER_SORT_SPEC.md` §5, gaps G4/G5 |

## Open Decisions Awaiting Director Input

- Final numeric performance targets (`09_TEST_STRATEGY.md` §9) — measurement categories are now fixed (D-008); numeric thresholds remain a working assumption until a real Lighthouse run exists (Phase 13). Phase 4's real WebGL scene raised Total Blocking Time noticeably (610ms → 1,140ms, desktop/unthrottled) — expected from continuous `useFrame` rendering work, flagged for Phase 13 consideration (e.g. `frameloop="demand"` when idle), not addressed now per the Director's explicit "do not optimize prematurely" instruction.
- ~~Whether Gemini-generated imagery should replace the Phase 5 procedural SVG assets (D-036).~~ **Specified in Phase 5B** (D-042–D-046): 70 assets approved, 56 deferred, 35 kept procedural, with prompts, provenance workflow and review gate authored. Still awaiting a Director decision to actually *run* the generation pass — the ownership/provenance question is now documented and handled (D-045) rather than open, but no image has been generated.
- Main-bundle growth from bundling the full content dataset: 367 kB → 493 kB raw (114 kB → 135 kB gzip) now that all content JSON is statically imported through `src/data/index.ts`. Acceptable at this size and untouched per the Director's "no full optimization in Phase 5" boundary, but per-category lazy loading is the obvious lever if Phase 13 needs it.
- Deployment/hosting target for the optional live URL (DR-008) — affects the `HashRouter` vs. `BrowserRouter` choice finality (D-002); `HashRouter` remains the Phase 1 implementation since it works on any static host without configuration.
| D-051 | 2026-09-24 | The landing page is composed from **presentational section components** in `src/sections/`, and `HomePage.tsx` is reduced to pure composition. `CinematicEntry` is **wrapped, never modified**. | Seven distinct bands of content on one route made `HomePage` the place where layout, data access and art direction would all collect. Splitting them keeps each band independently readable and testable, and keeps `HomePage` a table of contents for the page. `CinematicEntry` owns the page's only `<h1>` and the whole WebGL -> 2D -> links progressive-enhancement stack, with tests asserting that; wrapping it adds the cinematic backdrop and CTA row without putting any of that at risk. | Building the landing inline in `HomePage` (rejected — one file owning seven concerns); replacing `CinematicEntry` with a landing-specific hero (rejected — would have re-implemented the accessible fallback chain and broken its tests). | `src/sections/*` (7 new section components); `src/pages/HomePage.tsx`; `src/components/Reveal/`; `src/hooks/useScrollReveal.ts`; `src/hooks/useScrolled.ts`. |
| D-052 | 2026-09-24 | The runtime serves **derived WebP renditions**, not the approved master PNGs. Masters stay byte-identical on disk; `USE_MASTERS` in `src/data/landingAssets.ts` switches back in one line. | Measured: the landing page loaded **85.9 MB** of images, because it shows all fourteen Batch 01 masters (2048x2048 and 2752x1536, 5-8 MB each) in boxes no wider than 1480 CSS px. `scripts/build-web-assets.py` derives 1920px / 800px WebP renditions — same crop, framing and colour, only smaller — bringing the same page to **1.0 MB**. Nothing is generated, replaced, renamed or deleted, so the asset freeze holds; only what the browser is asked to download changes. | Serving the masters (rejected — indefensible page weight); resizing the masters in place (rejected — violates the freeze and is irreversible); `<picture>` with PNG fallback (rejected — WebP is universally supported by the four browsers under test, and the fallback would re-introduce the multi-megabyte path). | `scripts/build-web-assets.py` (new); `public/assets/gemini/web/` (21 renditions, 2.1 MB total); `src/data/landingAssets.ts`; `src/sections/FinalCta.tsx`. Enforced by the image-budget test in `e2e/landing.spec.ts`. |
| D-053 | 2026-09-24 | Three delivered filenames that differ from the manifest stems are absorbed by **a mapping table in `src/data/landingAssets.ts`**, not by renaming files or editing the manifest. | `character-movies-lena-cross` shipped as `...-det-lena-cross`, `...-elena-marsh` as `...-dr-elena-marsh`, and `...-aegis` as `...-aegis-marcus-steele`. The assets are frozen and the manifest is the provenance record, so neither may be rewritten to paper over the difference. One mapping table gives a single place to reconcile if the pipeline later normalises the names. | Renaming the delivered files (rejected — breaks the freeze and the provenance trail); editing the manifest (rejected — the manifest records what was generated, not what was convenient). | `src/data/landingAssets.ts`. `_gemini_batch_01/` and `docs/GEMINI_BATCH_01_MANIFEST.json` unchanged. |
| D-054 | 2026-09-24 | The Fandom Core's hero entrance animates **transform only, never opacity**. | The entrance originally faded the Core in from `opacity: 0`. That block carries the page's only `<h1>` and the seven category labels, so for the length of the animation those labels rendered at roughly 55% opacity — a measured 4.38:1 against the hero scrim, below the 4.5:1 WCAG AA minimum for 13px text. An axe run on load caught it intermittently. Text that fades from zero is always below AA on the way up, so the fix is to not fade it. | Slowing or delaying the fade (rejected — moves the window, does not close it); excluding the hero from the axe scan (rejected — that hides a real contrast failure rather than fixing it). | `src/sections/LandingHero.module.css` (`heroSettle` replaces `heroRise` for the Core). The CTA row and hero note still use `heroRise`; neither is below AA at rest. |
| D-055 | 2026-09-24 | `visualMotif` is **not rendered in the UI**. It stays art-direction metadata for the asset pipeline. | Category cards showed strings like "Ember gradients and blade-light streaks" above each title, wrapping to two lines. That copy was written to brief an image generator, not to be read by a visitor, and it competed with the category name for attention. The card's accent colour already carries the motif visually. | Shortening the motif strings (rejected — they are pipeline inputs; rewriting them for display would decouple them from the prompts they feed); truncating to one line (rejected — still meaningless to a reader). | `src/sections/CategoryExplorer.tsx`, `src/sections/CategoryExplorer.module.css`. `categories.json` unchanged — the field is still there for the asset pipeline. |
| D-056 | 2026-09-24 | The landing page has a **test-enforced image budget of 4 MB**. | The 85.9 MB regression in D-052 produced no broken image, no console error and no failing test — the page simply took a long time and looked fine locally. Page weight is invisible to every other kind of assertion in this suite, so it gets an explicit one. | Relying on review to catch it (rejected — it was not caught by review, it was caught by measuring); a tighter budget at the measured 1.0 MB (rejected — leaves no room for legitimate art revisions and would fail on unrelated changes). | `e2e/landing.spec.ts` ('stays within the landing image budget'). |
| D-057 | 2026-09-24 | The landing page is a **chapter sequence driven by one fractional progress float**. `useChapterProgress` writes `--chapter-progress` to the stage element once per animation frame; every chapter derives its own parallax from it in CSS. | A cinematic scroll page does not want 'which section is on screen' — a boolean per section gives cuts. It wants a continuous number whose integer part is the chapter and whose fraction is the travel out of it, so a single value can drive depth, atmosphere and the rail in step. Anchors are measured per chapter (its centre meeting the viewport centre) rather than taken from `scrollY / pageHeight`, so a tall chapter does not feel slow and a short one does not feel like a jump cut. The damped value never enters React state: at 60fps that would re-render the page on every frame. Only `activeIndex` is state, and it changes about ten times per page. | A scroll listener per section (rejected — N listeners and N re-renders); IntersectionObserver per chapter (rejected — gives a boolean, not travel); `animation-timeline: view()` (rejected — not supported across the four browsers under test). | `src/features/landing/useChapterProgress.ts`; every `src/sections/Chapter*` and `CategoryChapter` stylesheet. |
| D-058 | 2026-09-24 | **Kage's architecture is ported; none of its code or artwork is used.** | Two independent reasons. Runtime: Kage is ~3,600 lines of global-scope vanilla JavaScript against a vendored Three.js **r149**, while FandomVerse is React 19 with `three@0.186` and `@react-three/fiber`. Adopting it would ship two incompatible copies of Three.js and two renderers competing for WebGL contexts. Subject matter: all fourteen Kage images are Japanese temple-garden pieces — torii, sakura, stone lanterns, shrine ruins — and FandomVerse's seven worlds are an anime ronin epic, a tactical shooter, a noir thriller, an archive mystery, a K-pop group, a superhero comic and a supernatural manga. Sakura behind LUNARIS would not read as one universe. Separately, the Kage repository grants no licence for reuse (`README` §License), so using its assets would depend entirely on a private grant. | Installing via `threeui-cli` (rejected — the only npm package of that name is third-party community software from a different owner than `MengTo`, and Kage is catalogued as a full-page document with no registry entry); embedding Kage in an iframe (rejected — no shared routing, data or design tokens). | The whole `src/features/landing/` and chapter section set is original. `docs/KAGE_FANDOMVERSE_INTEGRATION.md` holds the full audit. |
| D-059 | 2026-09-24 | The environmental layer is a **2D canvas**, not a second WebGL context. | The page already runs one WebGL context for the Fandom Core. A second competing for contexts is a real failure mode on low-end devices, and a full 3D environment would cost far more than this page can justify. What the composition actually needs from a 'camera' is depth parallax and atmosphere, and a few hundred depth-sorted motes give that for a few kilobytes, no new dependency, and no context-loss risk. DPR is capped at 2 (1.5 on small screens) and mote count scales by viewport area. | A second `@react-three/fiber` canvas (rejected — two WebGL contexts, and the 912 kB chunk would become blocking rather than lazy); CSS-only particles (rejected — hundreds of animated DOM nodes cost more than one canvas). | `src/features/landing/AtmosphereCanvas.tsx` + `.module.css`. |
| D-060 | 2026-09-24 | Each chapter is a **sticky, viewport-height stage inside a taller section**, so the shot arrives, holds, and releases. | This is what makes the page read as travel rather than as a stack of panels: nothing cuts, the camera simply stops pushing while the reader moves through the chapter's extra height. It needs no JavaScript at all. The stage is pinned below the measured sticky header and sized to what is actually visible, so the composition centres in the frame the reader sees rather than in a box running under the header. | Scroll-jacking / snap points (rejected — takes control of scrolling away from the reader and breaks keyboard and trackpad expectations); fixed positioning with manual show/hide (rejected — that is exactly the hard cut this architecture exists to avoid). | `src/sections/CategoryChapter.module.css`; `--header-h` published by `useChapterProgress`. |
| D-061 | 2026-09-24 | Chapter calls to action are named for the **franchise** ("Enter Starlit Ronin"), never "Explore {Category}". | The Fandom Core already owns a link named `Explore {Category}` for all seven categories, and `e2e/fandom-core.spec.ts` clicks them by that name. A second link sharing both the name and the destination is a strict-mode violation for the test and, more importantly, is indistinguishable from the first in a screen reader's link list. Franchise names are unique across the dataset, so they disambiguate without inventing copy. | "Explore {Category}" on the chapter too (rejected — duplicate accessible name and destination); "Learn more" (rejected — seven identical link names, which is the same problem made worse). | `src/sections/CategoryChapter.tsx`; asserted by `e2e/landing.spec.ts`. |
| D-062 | 2026-09-24 | **No `will-change` on the chapter plates or figures**, and `will-change` released after any one-shot reveal. | With seven chapters, `will-change: transform` promoted fourteen permanent composited layers on top of seven sticky stages and a full-viewport canvas. Under that pressure Chromium stopped rasterising some of them: the affected element kept its bounding box, its computed opacity, its decoded image and its position at the top of `elementsFromPoint`, and painted nothing. Characters were missing when a visitor scrolled to them on a 375px phone, with no console error, no failed request and no failing assertion. Confirmed by toggling `will-change` to `auto` live in the browser, which brought the figure back (region brightness 29 → 59). The transforms are scroll-driven and perform identically without the hint. | Keeping the hint and reducing layer count elsewhere (rejected — the sticky stages and the canvas are load-bearing); `contain: paint` on the stages (rejected — does not address layer memory, and risks clipping the feathered figure). | `src/sections/CategoryChapter.module.css`; `src/features/landing/WordReveal.module.css` releases it per word after reveal. |
| D-063 | 2026-09-24 | Anything in the opening screen **reveals on mount**, via `Reveal`'s new `immediate` prop, never on intersection. | `useScrollReveal` uses `rootMargin: '0px 0px -8% 0px'` with `threshold: 0.15`. At 1024x768 the intro's statement sat inside that negative margin: already on screen, never enough of it inside the shrunken root to cross the threshold, so it stayed at `opacity: 0` permanently. "Enter the universe" — the hero's central line — simply never appeared. An observer is the wrong tool for content that is visible before any scroll happens, which is also how the reference architecture handles its hero. | Widening the rootMargin or lowering the threshold (rejected — moves the dead zone to some other viewport instead of removing it); dropping the reveal from the intro entirely (rejected — the staged entrance is the point of a threshold). | `src/components/Reveal/Reveal.tsx`; `src/sections/ChapterIntro.tsx`. |
| D-064 | 2026-09-24 | The Fandom Core's height on the landing is driven by **viewport height**, not width. | The threshold has to fit a chapter marker, the wordmark, the Core, a statement and a scroll cue into one fold. What constrains that is vertical room, and vertical room does not track width: a 1280x800 desktop has less of it than a 768x1024 tablet. A width-based rule gave the tablet a Core too short for its own orbit ring (clipped left and right with ~290px of the fold empty) while giving short desktops one too tall to fit the cue. | A single fixed height (rejected — that is what produced both failures); scaling the Core with `transform` (rejected — it would scale the category labels' text with it, below their legible size). | `src/sections/ChapterIntro.module.css`: a `max-height: 860px` rule and a tablet-range rule, both setting the `--core-height` hook that `CinematicEntry.module.css` exposes (default unchanged). |
